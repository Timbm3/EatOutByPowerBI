@model IEnumerable<EatOutByBI.Data.Classes.BookingTime>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Time)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateCreated)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateModified)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BookingTimeId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Time)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateCreated)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateModified)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BookingTimeId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BookingTimeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BookingTimeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BookingTimeId })
        </td>
    </tr>
}

</table>
