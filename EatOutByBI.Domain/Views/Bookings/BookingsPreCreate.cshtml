@model  EatOutByBI.Data.DTO.BookingDTO

@using System.Web.Script.Serialization

@{
    ViewBag.Title = "BookingsPreCreate";
}

<div class="standardMenu">
    <h2>Välj Datum</h2>

    @using (Html.BeginForm("BookingsPreCreate", "Bookings", new { id = Model.BookedId }, FormMethod.Post, new { }))
    {
        @Html.AntiForgeryToken()

        @*@Html.DisplayFor(model => model.BookedId)*@
        <div>
            @*@Html.EditorFor(model => model.Date, new { htmlAttributes = new { @class = "form-control date-picker" } })*@
            @Html.EditorFor(model => model.Date, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.Date, "", new { @class = "text-danger" })
            <div class="date-picker"></div>
            @*<input type="button"  class="testus" value="test" />*@
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <button type="submit" class="btn btn-default">@Html.ActionLink("Nästa", "BookingsCreate", new { id = Model.BookingId })</button>
                @*<input id="btnCreate" type="submit" value="Create" class="btn btn-default"/>*@
            </div>
        </div>
        <br />
        <br />
        <br />
        <br />
    }



</div>

@section Scripts{
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/Scripts/AppScripts.js")
}
