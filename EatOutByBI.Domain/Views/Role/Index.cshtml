
@{
    ViewBag.Title = "Till till Roll";
}

<h2>Index</h2>

@model IEnumerable<Microsoft.AspNet.Identity.EntityFramework.IdentityRole>
@{
    ViewBag.Title = "Lägg Till Roll";
}

<div class="row">
    <div class="col-sm-3 menuNav">
        @Html.Partial("_AdminPagePartial")
        @*<p class="onMenuChoice">@Html.ActionLink("Admin", "Index", "AdminPage")</p>
        <p>@Html.ActionLink("Lägg till Prod/Anställd", "AdminAddProdEmp", "AdminPage")</p>
        <p>@Html.ActionLink("Lägg till Användar Roll", "Index", "Role")</p>
        <p>@Html.ActionLink("Registrera Användare", "Register", "Account")</p>*@
    </div>
    <div class="col-sm-7 tblAddRole standardMenu">
        <div>
            <p>Här nedanför ser ni villka roller som finns, vill ni skapa nya kan du göra det här nedanför</p>
        </div>
        <h3> @Html.ActionLink("Tryck för att skapa ny Roll", "Create", "Role") </h3>



        <table class="table">
            <tr>
                <th>
                    <h3>Roller</h3>
                </th>

            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>

                </tr>
            }

        </table>

        @*<table cellpadding="6" cellspacing="6">
            <tr id="tblHead">
                <td colspan="2">
                    <h2> Create User Roles</h2>
                </td>

            </tr>
            <tr>
                <td>
                    <table id="tblRoles">

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td id="tblName">
                                    @item.Name
                                </td>
                            </tr>}
                    </table>
                </td>
                <td>


                </td>
            </tr>
        </table>*@
    </div>
</div>

