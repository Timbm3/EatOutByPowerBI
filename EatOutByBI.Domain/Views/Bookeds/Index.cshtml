@model IEnumerable<EatOutByBI.Data.Classes.Booked>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DateAndTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Plats)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BookingTimeId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsAvailable)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DateAndTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Plats)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BookingTimeId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsAvailable)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BookedId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BookedId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BookedId })
        </td>
    </tr>
}

</table>
