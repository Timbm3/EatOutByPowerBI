@model IEnumerable<EatOutByBI.Data.DTO.PaymentMethodDTO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table standardMenu">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PaymentMethodType)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PaymentMethodType)
        </td>

        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PaymentMethodId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PaymentMethodId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PaymentMethodId })
        </td>
    </tr>
}

</table>
